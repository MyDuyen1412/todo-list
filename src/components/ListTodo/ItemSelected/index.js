import React from "react";

const ItemSelected = ({ item, className }) => {
//   if (item) return null;
console.log(className)
  return (
    <div>
      {/* <p>{item.title}</p>
      <p>{item.content}</p> */}
      abc
    </div>
  );
}; 

export default ItemSelected;
